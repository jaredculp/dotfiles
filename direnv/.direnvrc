#!/bin/bash
set -e

java_version() {
  [[ -f "$PWD/.java-version" ]] && cat "$PWD/.java-version"
}

java_env() {
  j="$(java_version)"
  if [[ ! -z "$j" ]]; then
    echo "Using java $j"
    JAVA_HOME="$(/usr/libexec/java_home -v "$j")"
    export JAVA_HOME
  fi
}
