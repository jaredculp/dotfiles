#!/usr/bin/env bash

#
# cmus-current displays the currently playing song in cmus.
# 

set -e
set -o pipefail

cmus_status() {
  /usr/local/bin/cmus-remote --raw status | grep "$1" | cut -d ' ' -f "3-"
}

title=$(cmus_status "title")
album=$(cmus_status "album")

echo -n "${title} • ${album}"
